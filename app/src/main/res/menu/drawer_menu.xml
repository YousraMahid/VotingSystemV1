<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

<group android:checkableBehavior="single">
            <item
                android:id="@+id/nav_profile"
                android:icon="@drawable/ic_person_grey"
                android:title="Profile" />
            <item
                android:id="@+id/nav_notification"
                android:icon="@drawable/ic_notifications_grey"
                android:title="Notification" />
            <item
                android:id="@+id/nav_setting"
                android:icon="@drawable/ic_settings_grey"
                android:title="Setting" />
            <item
                android:id="@+id/nav_help"
                android:icon="@drawable/ic_help_grey"
                android:title="Help" />

            <item
                android:id="@+id/nav_feedback"
                android:icon="@drawable/ic_feedback_grey"
                android:title="Feedback" />

            <item
                android:id="@+id/nav_about"
                android:icon="@drawable/ic_info_grey"
                android:title="About" />

            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout_grey"
                android:title="LogOut" />

        </group>

</menu>